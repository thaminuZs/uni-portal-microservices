services:

  mongo:
    image: mongo
    container_name: mongo_uniportal
    command: ["mongod", "--quiet"]
    ports:
      - "28015:27017"
    volumes:
      - mongo_data:/data/db

  lecturer-service:
    build: ./services/lecturer-service
    ports:
      - "4002:4002"
    environment:
      - PORT=4002
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongo:27017/lecturer-db
    depends_on:
      - mongo

  canteen-service:
    build: ./services/canteen-service
    ports:
      - "4001:4001"
    environment:
      - PORT=4001
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongo:27017/canteen-db
    depends_on:
      - mongo

  library-service:
    build: ./services/library-service
    ports:
      - "4003:4003"
    environment:
      - NODE_ENV=production
      - PORT=4003
      - MONGO_URI=mongodb://mongo:27017/library-db
    depends_on:
      - mongo

  uniportal-gateway:
    build: ./gateway
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
      - LECTURER_URL=http://lecturer-service:4002
      - CANTEEN_URL=http://canteen-service:4001
      - LIBRARY_URL=http://library-service:4003
    depends_on:
      - lecturer-service
      - canteen-service
      - library-service

  dozzle:
    container_name: dozzle
    image: amir20/dozzle
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - 8888:8080

volumes:
  mongo_data: